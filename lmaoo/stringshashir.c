#include <stdio.h>
#include <stdlib.h>
int main()
{
    system("cls");
    char lamo[15];
    gets(lamo);
    printf("%s", lamo);
    char kk[79];
    gets(kk);
    puts(kk);
    long long l;
    scanf("%d",l);
    
}